// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0){
        printf("second cannot be zero");
        b=1;
    }
    int r=a/b;
    printf("\n%d",r);
    // Write C code her;e


    return 0;
}
