#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    int r1=2*(a+b);
    printf("%d\n",r1);
    int r2=(a*b);
    printf("%d",r2);
          
          

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
